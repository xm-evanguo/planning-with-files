# planning-with-files

- `skills/planning-with-files` is the source of truth for the Codex-compatible skill in this repo.
- Keep `.codex/skills/planning-with-files` and any packaged copies aligned with that folder.
- If `SKILL.md` changes meaningfully, update `docs/codex.md` and the matching README sections in the same change.
- Keep `skills/planning-with-files/agents/openai.yaml` aligned with `SKILL.md`.
